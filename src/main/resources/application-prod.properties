spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/clinipets}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:password}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect


spring.jpa.hibernate.ddl-auto=update
logging.level.root=INFO
logging.level.cl.clinipets=INFO
# --- JWT CORREGIDO ---
# Usa los nombres que tu c�digo espera:
jwt.expiration-minutes=60
jwt.refresh-expiration-hours=168
jwt.issuer=ClinipetsBackend
jwt.cookie-name=clinipets-refresh